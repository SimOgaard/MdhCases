" ".join(list)

str.split(" ")

if num % 2 == 0:

list.insert(3,"General Motors")

json.dumps(list)

json.loads(str)

def largest(*larg):
    return max(larg)

if type(lol) is str

def increment(li):
    return [x+1 for x in li]

import json
print(sum(json.loads(open('todos.json').read())["integers"]))

def itterate(ITER):
    for key, value in ITER.items():
        if type(value) is list:
            for items in value:
                itterate(items)
        else:
            print(key+": "+value)